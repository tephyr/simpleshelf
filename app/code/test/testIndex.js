/**
 * NOTE: until PhantomJS supports ES6 (expected in v2.5), all unit test code
 * and **all code under test** must use ES5 syntax.
 * This means that code not converted by babel (let/const, currently) is *not supported*.
 */
require("./models/test.Book.js");
require("./models/test.Configuration.js");
require("./views/test.EditBookPageView.js");
