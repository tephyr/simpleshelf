# production settings
version: "3.1"
 
services:
        
    node:
      image: ${DOCKER_ACCT}/simpleshelf-node-front:production
      ports:
        # Customize exported port.
        - "9090:8080"

    couchdb:
      ports:
        # Disallow external access.
        - "5984"
